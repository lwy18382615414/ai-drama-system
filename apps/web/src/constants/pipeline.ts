/**
 * Ordered workflow-page tabs used by the pipeline strip. One entry per
 * workbench page, labels identical to the sidebar so the strip and the nav
 * always speak the same names.
 */
export const pipelineSteps = [
  { key: 'novel', label: '小说与事件' },
  { key: 'episodes', label: '剧集规划' },
  { key: 'script', label: '剧本编辑' },
  { key: 'assets', label: '角色/场景/道具' },
  { key: 'storyboard', label: '分镜工作台' },
  { key: 'image', label: '角色参考图' },
]
