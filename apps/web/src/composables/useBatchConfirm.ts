/**
 * Two-stage batch confirmation: show count → confirm modal → create Job → open drawer.
 * NOTE: the cost/amount UI is explicitly removed from this design (README §Interactions),
 * so this is the simplified `useBatchConfirm` (not `useCostConfirm`) — count only, no ¥.
 *
 * TODO: implement confirm flow + job creation.
 */
export function useBatchConfirm() {
  // TODO
  return {}
}
