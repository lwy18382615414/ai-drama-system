# Backend origin the Vite dev proxy forwards /api and /static to.
# Defaults to http://localhost:3000 when unset.
VITE_BACKEND_ORIGIN=http://localhost:3000

# Axios baseURL. Defaults to /api (which the dev proxy forwards to the backend).
# Override only when calling the backend directly (no proxy).
VITE_API_BASE_URL=/api
